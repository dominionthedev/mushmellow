version: 1

name: Mush Example

env:
  GOFLAGS: -mod=readonly

ci:
  compatible: true
  output: junit

mushmellows:

  test-timeout:
    description: Test timeout behavior
    puffs:
      - id: slow-puff
        run: sleep 5
        timeout: 1s
